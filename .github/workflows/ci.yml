name: CI

on:
  pull_request:
    branches: [ main, dev ]
  push:
    branches: [ dev, test ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    uses: Management-System-for-Rental-SEP490/.github/.github/workflows/ci-python-service.yml@main
